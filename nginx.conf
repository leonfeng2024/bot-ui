server {
    listen 80;
    server_name jptyomdmidd001.onetakeda.com;

    # 通用错误页面
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # 项目1: Html_Js_Css (静态文件，使用端口 8081)
    location /v1/ {
        alias /app_mdm/work_space/bot-ui/Html_Js_Css/;
        index index.html;
        try_files $uri $uri/ /v1/index.html;
    }

    # 项目2: Vue2_Vuetify2 (使用端口 8082)
    location /v2/ {
        proxy_pass http://localhost:8082/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 项目3: React_Bootstrap3 (使用端口 8083)
    location /v3/ {
        proxy_pass http://localhost:8083/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # API 代理
    location /chat {
        proxy_pass http://localhost:8000/chat;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
} 